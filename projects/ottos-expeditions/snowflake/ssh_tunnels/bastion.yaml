ssh_tunnel:
  host: dataconn.copera.org
  port: 22
  user: ascend
  private_key: ${vaults.intance.bastion_private_key}
connection:
  ssh_tunnel:
    host: dataconn.copera.org
    port: 22
    user: ascend
    private_key: ${vaults.instance.bastion_private_key}
